# Sign Language Recognition (MediaPipe + Keras + Streamlit)

## Overview
This project recognizes static sign language gestures using MediaPipe hand landmarks and a Keras model. It is free and easily deployable locally or on Streamlit (with minor changes).

## Setup
1. Create a virtual environment and install:
   ```bash
   python -m venv venv
   source venv/bin/activate   # or venv\Scripts\activate on Windows
   pip install -r requirements.txt
